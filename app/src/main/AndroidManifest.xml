<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.upp_jobs">

    <!-- Permiso de Internet necesario para realizar solicitudes HTTP -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Upp_jobs"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Actividad principal: RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.Upp_jobs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- PostActivity -->
        <activity
            android:name=".PostActivity"
            android:exported="true" />

        <!-- CrearActivity -->
        <activity
            android:name=".CrearActivity"
            android:exported="true" />

        <!-- CrearActivity -->
        <activity
            android:name=".UsersActivity"
            android:exported="true" />

        <!-- CrearActivity -->
        <activity
            android:name=".HomeFragment"
            android:exported="true" />

        <!-- CrearActivity -->
        <activity
            android:name=".NotificationsFragment"
            android:exported="true" />

        <!-- CrearActivity -->
        <activity
            android:name=".ProfileFragment"
            android:exported="true" />

        <!-- CrearActivity -->
        <activity
            android:name=".StoryDetailActivity"
            android:exported="true" />

        <activity
            android:name=".FriendsActivity"
            android:exported="true" />

        <activity
            android:name=".MarketPlace"
            android:exported="true" />

        <activity
            android:name=".NotificationsActivity"
            android:exported="true" />

        <activity
            android:name=".SettingsActivity"
            android:exported="true" />

        <activity
            android:name=".Activity_Recuperar"
            android:exported="true" />

        <activity
            android:name=".EditPostActivity"
            android:exported="true" />

        <activity
            android:name=".CrearPostActivity"
            android:exported="true" />

        <activity
            android:name=".ResetPasswordActivity"
            android:exported="true" />

        <activity
            android:name=".VerifyResetCodeActivity"
            android:exported="true" />

        <activity
            android:name=".Activity_editar"
            android:exported="true" />

    </application>
</manifest>
